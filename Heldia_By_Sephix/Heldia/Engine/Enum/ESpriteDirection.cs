namespace Heldia.Engine.Enum;

/// <summary>
/// Set a name of each number which
/// correspond to them lines n the sprite
/// </summary>
public enum ESpriteDirection
{
    Idle = 0,
    North = 3,
    East = 1,
    South = 4,
    West = 2
}